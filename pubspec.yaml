name: syncit
description: "local file-syncing application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: 3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.8
  watcher: 1.1.1
  network_info_plus: ^6.1.4
  path: ^1.8.3
  provider: ^6.1.1
  file_picker: ^6.1.1
  shared_preferences: ^2.5.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

